A square icon-only control for toolbar/header actions (menu, search, close). Always pass `label` — it becomes the accessible name.

```jsx
<IconButton icon="search" label="Search" variant="ghost" />
<IconButton icon="menu" label="Open menu" variant="secondary" />
```

Minimum target is 44×44 CSS px per the guidelines' Iconography section, even if `size` is set smaller.
