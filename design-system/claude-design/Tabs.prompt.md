A single stroke-based glyph from Phaeno's curated Lucide-derived icon set — use for interface controls, never as a substitute for scientific evidence.

```jsx
<Icon name="arrowRight" size={16} />
<Icon name="microscope" size={24} color="var(--color-brand)" />
```

Notes:
- Icons are simple outlines, 24×24 viewBox, 2px stroke weight by default — keep consistent stroke weight across a screen.
- Minimum interactive target when an icon is the whole control: 44×44 CSS px (wrap in IconButton).
- Pair unfamiliar icons with a visible label.
- Full glyph list: menu, x, search, arrowRight, arrowUpRight, arrowLeft, chevronDown, chevronRight, chevronLeft, check, alertTriangle, info, download, mail, phone, externalLink, plus, minus, user, circleCheck, circleX, loader2, microscope, flaskConical, fileText, database, activity, lock, settings, calendar, clock, trash2, pencil, copy, filter, building2, globe, dna.
