A small pill for status/category labels — RUO, preliminary, validated, phase markers.

```jsx
<Badge tone="evidence">RUO</Badge>
<Badge tone="brand">Validated</Badge>
```

Tones map to semantic roles, not raw palette. Never rely on tone/color alone to convey validation status — pair with text (per Accessibility guidance).
