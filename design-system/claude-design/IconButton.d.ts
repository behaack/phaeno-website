The primary action control — labels start with a verb and name the outcome (`Request a demo`, `Read the study`).

```jsx
<Button variant="primary">Request a demo</Button>
<Button variant="secondary" icon="arrowRight">Read the study</Button>
<Button variant="tertiary" icon="arrowRight">See performance data</Button>
```

Variants: `primary` (accessible green fill, white label — one per region), `secondary` (white surface, strong neutral border), `tertiary` (text + directional arrow, action-orange). Sizes: `sm` / `md` / `lg`. Disabled state reduces opacity and sets `aria-disabled`.
