Custom-styled checkbox (visually hidden native input for full accessibility).

```jsx
<Checkbox label="I have IRB approval for this sample set" checked={ok} onChange={(e) => setOk(e.target.checked)} />
```
