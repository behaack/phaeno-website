Labeled text field. Labels stay visible (placeholders are examples, not labels); required state is textual, not color-only.

```jsx
<Input label="Work email" placeholder="you@lab.edu" required />
<Input label="Sample ID" error="Sample ID is required" />
```
